# Mini-Auftragsverwaltung mit PHP und JSON

Lernziel: einfache Business-Anwendung mit Formular, Status, JSON-Persistenz und Berechnung von Netto, MwSt. und Brutto.

Start:

```bash
php -S localhost:8004 -t public
```
